// Utilizarei operador ternário em uma arrow function

const aprovado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado';
console.log(aprovado(7));
console.log(aprovado(6));

/* O operador ternário tem forma :
(expressão) ? 'Saída, caso true' : 'Saída, caso false'
*/