let areaRetangulo = (lado1, lado2) => {return (lado1 * lado2).toFixed(2)}
let areaTriangulo = (base, h) => {return ((base * h) / 2).toFixed(2)}
let areaTrapezio = (B, b, h) => {return ((B + b) * h / 2).toFixed(2)}
let areaCirculo = raio => {return (Math.PI * Math.pow(raio, 2)).toFixed(2)}
let raiz1Grau = (a, b = 0) => {return (- 1 * b / a).toFixed(2)}
